module Control.Effect.Select
  ( -- * Effect
    Select(..)

    -- * Actions
  , select

    -- * Interpretations
  , runSelect

    -- * Carriers
  , SelectC
  ) where

import Control.Effect.Internal.Select
